**What does this PR do?**

**Checklist**
- [ ] Tests added/updated and passing offline (`uv run pytest` — no keys, no network)
- [ ] `uv run ruff check .` passes
- [ ] CHANGELOG.md updated (user-visible changes)
